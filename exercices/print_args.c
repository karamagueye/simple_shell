#include "main.h"
/**
 * main - prints all arguments without using ac
 * Return: 0 (success)
 */
int main(int ac, char **av)
{
	int i;

	for (i = 0; av[i][j] != '\n', i++)
	{
		printf("%s\n", (*av + i));
	}
}
